<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>sla</artifactId>
		<groupId>tmf.org.dsmapi</groupId>
		<version>1.0-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	
	<groupId>tmf.org.dsmapi</groupId>
	<artifactId>sla-ear</artifactId>
	<packaging>ear</packaging>
	
	<name>${project.artifactId}(${project.packaging})</name>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Generated by Netbeans -->
		<netbeans.hint.deploy.server>gfv3ee6</netbeans.hint.deploy.server>
	</properties>

	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>sla-war</artifactId>
			<type>war</type>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-ear-plugin</artifactId>
				<configuration>
					<version>6</version>
					<!-- Java EE 6 -->
					<defaultLibBundleDir>lib</defaultLibBundleDir>
					<modules>
						<webModule>
							<groupId>${project.groupId}</groupId>
							<artifactId>sla-war</artifactId>
							<contextRoot>DSSlaStandalone</contextRoot>
						</webModule>
					</modules>
				</configuration>
			</plugin>    
			<!--   Automatic deployment of the app on Glassfish -->
            <!--<plugin>
                <groupId>org.glassfish</groupId>
                <artifactId>maven-embedded-glassfish-plugin</artifactId>
                <configuration>
                    <goalPrefix>glassfish</goalPrefix>
                    <app>target/${project.artifactId}-${project.version}.${packaging}</app>
                    <port>8080</port>
                    <contextRoot>DSSlaStandalone</contextRoot>
                    <name>DSSlaStandalone</name>
					<createTables>true</createTables >
                </configuration>
                <executions> 
					<execution>
						<id>gf-undeploy</id>
                        <goals>
                            <goal>undeploy</goal>
                        </goals>
                        <phase>clean</phase>
                    </execution>
                    <execution>
						<id>gf-deploy</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
				<dependencies>
					<dependency>
						<groupId>com.h2database</groupId>
						<artifactId>h2</artifactId>
						<version>1.3.155</version>
				</dependency>  
			</dependencies>
            </plugin>-->
		</plugins>
	</build>
</project>
